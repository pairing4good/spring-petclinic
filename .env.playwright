# Playwright Configuration

# Browsers to install (only Chromium needed for CI)
PLAYWRIGHT_BROWSERS_PATH=./playwright-browsers

# Skip browser download during Maven/Gradle build
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1

# Use bundled browsers when running in CI
PLAYWRIGHT_CI=1